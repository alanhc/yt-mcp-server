# YouTube 頻道總結功能 - 快速設置指南

## 🎯 功能說明

這個新功能可以：
1. 自動獲取 YouTube 頻道的最新影片
2. 提取所有影片的字幕
3. 使用 Claude AI 生成繁體中文總結

## 📋 前置需求

### 1. Anthropic API Key

您需要一個 Anthropic API key 來使用 Claude AI。

**獲取步驟：**
1. 訪問 https://console.anthropic.com/
2. 註冊或登入帳號
3. 前往 API Keys 頁面
4. 創建新的 API key
5. 複製 API key（格式：`sk-ant-...`）

### 2. 設置環境變量

在項目根目錄創建或編輯 `.env` 文件：

```bash
# .env
ANTHROPIC_API_KEY=sk-ant-your-actual-api-key-here
```

**注意：** 不要將 `.env` 文件提交到 git！

## 🚀 使用方式

### 方法 1：使用測試腳本

```bash
# 確保已設置 ANTHROPIC_API_KEY
uv run test_channel_summary.py
```

### 方法 2：直接調用 API

```bash
curl -X POST http://localhost:8000/youtube/channel/summary \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://www.youtube.com/@yutinghaofinance/streams",
    "max_videos": 3
  }'
```

### 方法 3：Python 代碼

```python
import requests

response = requests.post(
    "http://localhost:8000/youtube/channel/summary",
    json={
        "url": "https://www.youtube.com/@yutinghaofinance/streams",
        "max_videos": 5,
        "custom_prompt": "請總結這些影片的主要投資建議"
    }
)

result = response.json()
print(result['summary'])
```

## 📊 API 參數說明

| 參數 | 類型 | 必填 | 說明 |
|------|------|------|------|
| `url` | string | ✅ | YouTube 頻道 URL |
| `max_videos` | integer | ❌ | 分析影片數量（預設：5，最大：10）|
| `custom_prompt` | string | ❌ | 自定義總結提示詞 |

## 📝 支持的 URL 格式

✅ 支持的格式：
- `https://www.youtube.com/@username`
- `https://www.youtube.com/@username/videos`
- `https://www.youtube.com/@username/streams`
- `https://www.youtube.com/channel/UC...`
- `https://www.youtube.com/c/channelname`

## 🔧 故障排除

### 問題 1：503 錯誤 - AI summarization not available

**原因：** 未設置 ANTHROPIC_API_KEY

**解決方案：**
1. 確認 `.env` 文件存在
2. 確認 API key 格式正確
3. 重啟服務器：`./start_server.sh`

### 問題 2：處理時間過長

**原因：** 分析多個影片需要時間

**解決方案：**
- 減少 `max_videos` 數量
- 使用異步處理（未來功能）
- 檢查網絡連接

### 問題 3：某些影片沒有字幕

**原因：** 影片可能沒有字幕或字幕不可用

**解決方案：**
- 系統會自動跳過無字幕的影片
- 查看返回結果中的 `videos_processed` 了解詳情

## 💡 使用建議

1. **首次測試**：從 3 部影片開始（`max_videos: 3`）
2. **自定義提示**：根據需求調整 `custom_prompt`
3. **成本控制**：Claude API 按 token 計費，注意控制使用量
4. **緩存利用**：字幕會被緩存，重複請求不會重新提取

## 📈 預期處理時間

| 影片數量 | 預估時間 |
|---------|---------|
| 3 部 | ~30 秒 |
| 5 部 | ~50 秒 |
| 10 部 | ~2 分鐘 |

*實際時間取決於影片長度和網絡速度*

## 🎯 使用場景

- **投資研究**：總結財經頻道的投資建議
- **學習筆記**：整理教學頻道的重點
- **新聞追蹤**：快速了解新聞頻道的最新動態
- **內容策劃**：分析競爭對手的內容方向

## 📞 需要幫助？

如果遇到問題：
1. 檢查服務器日誌
2. 確認 API key 有效
3. 測試單個影片的字幕提取是否正常
4. 查看 `FEATURES.md` 了解更多詳情
